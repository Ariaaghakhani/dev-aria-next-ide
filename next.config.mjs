/** @type {import('next').NextConfig} */
const nextConfig = {
    outputDir: "export",
    trailingSlash: true,
    basePath: "",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
